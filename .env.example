# Copy this file to .env and fill in your Anthropic API key.
# This variable is NOT exposed to the browser — it is only used by the
# Vite dev-server proxy, which injects it as an HTTP header.
ANTHROPIC_API_KEY=sk-ant-...
